# wordRec
There is a kmeans clustering model used, a word to vector model inside the cluster and apart from that a pickle is create of entities of each document and there vectors for mapping similar entities.
This project aims to recommend similar words with the help of kmeans clustering, word2vec and spacy.
